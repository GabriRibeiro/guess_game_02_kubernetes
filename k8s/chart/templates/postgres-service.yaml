apiVersion: v1
kind: Service
metadata:
  name: guess-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    app: guess-postgres
spec:
  selector:
    app: guess-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
