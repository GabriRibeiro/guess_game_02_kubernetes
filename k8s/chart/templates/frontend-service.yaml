apiVersion: v1
kind: Service
metadata:
  name: guess-frontend
  namespace: {{ .Release.Namespace }}
  labels:
    app: guess-frontend
spec:
  selector:
    app: guess-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.frontend.nodePort }}
  type: NodePort
